---
- name: Create LXC
  ansible.builtin.include_tasks: create.yml
  when: pve_lxc_lifecycle_action == "create"
  tags: lifecycle

- name: Start LXC
  ansible.builtin.include_tasks: start.yml
  when: pve_lxc_lifecycle_action == "start"
  tags: lifecycle

- name: Stop LXC
  ansible.builtin.include_tasks: stop.yml
  when: pve_lxc_lifecycle_action == "stop"
  tags: lifecycle

- name: Delete LXC
  ansible.builtin.include_tasks: delete.yml
  when: pve_lxc_lifecycle_action == "delete"
  tags: lifecycle
