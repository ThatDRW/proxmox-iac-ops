---
os_base_update_packages: true

os_base_common_packages:
  - curl
  - ca-certificates
  - bash

os_base_debian_packages:
  - python3
  - python3-apt
  - sudo

os_base_alpine_packages:
  - python3
  - py3-pip
  - sudo